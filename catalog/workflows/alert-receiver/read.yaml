schema: urn:f5xc:console:workflow:v1
id: alert-receiver-read
label: Read Alert Receivers
resource: alert-receiver
operation: read
params:
  namespace:
    required: true
    example: demo
  name:
    required: true
    example: example-alert-receiver
steps:
  - id: navigate-to-list
    action: navigate
    url: /web/workspaces/audit-logs-and-alerts/namespaces/system/manage/alert_receivers
    wait_for: text('Alert Receivers')
    description: Navigate to Alert Receivers list page
  - id: click-resource
    action: click
    selector: row:has-text('{name}') >> link
    description: Click the resource name to open the detail/edit view
    note: The agent reads the page content via get_page_text/javascript_tool after navigation
metadata:
  confidence: generated
  console_version: "2025.06"
  generated_by: scripts/generate-workflows.ts
