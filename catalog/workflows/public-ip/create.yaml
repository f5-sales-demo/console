schema: urn:f5xc:console:workflow:v1
id: public-ip-create
label: Create Public IP Addresses
resource: public-ip
operation: create
preconditions:
  - user_logged_in
  - namespace_selected
  - "role_minimum: admin"
params:
  namespace:
    required: true
    description: Target namespace
    example: demo
  name:
    required: true
    description: Public IP Addresses name (lowercase alphanumeric and hyphens)
    example: example-public-ip
steps:
  - id: navigate-to-list
    action: navigate
    url: /web/workspaces/web-app-and-api-protection/namespaces/{namespace}/manage/public_ip
    wait_for: text('Public IP Addresses')
    description: Navigate to Public IP Addresses list page
  - id: click-add-tab
    action: click
    selector: tab:text('Add Public IP')
    wait_for: textbox[name='Name']
    description: Click the Add Public IP tab to open the create form
  - id: fill-name
    action: fill
    selector: textbox[name='Name']
    value: "{name}"
    description: Enter Name
  - id: save
    action: click
    selector: "[class*='save-bt']"
    context: footer
    wait_for: text('{name}')
    wait_timeout_ms: 30000
    description: Save via the footer button (never use button:text — it collides with the header tab)
postconditions:
  - resource_list_page_visible
  - "resource_name_in_list: {name}"
metadata:
  confidence: generated
  console_version: "2025.06"
  generated_by: scripts/generate-workflows.ts
