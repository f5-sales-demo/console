schema: urn:f5xc:console:workflow:v1
id: site-mesh-group-read
label: Read Site Mesh Groups
resource: site-mesh-group
operation: read
params:
  namespace:
    required: true
    example: demo
  name:
    required: true
    example: example-site-mesh-group
steps:
  - id: navigate-to-list
    action: navigate
    url: /web/workspaces/multi-cloud-network-connect/manage/networking/site_to_site_connectivity/site_mesh_groups
    wait_for: text('Site Mesh Groups')
    description: Navigate to Site Mesh Groups list page
  - id: click-resource
    action: click
    selector: row:has-text('{name}') >> link
    description: Click the resource name to open the detail/edit view
    note: The agent reads the page content via get_page_text/javascript_tool after navigation
metadata:
  confidence: generated
  console_version: "2025.06"
  generated_by: scripts/generate-workflows.ts
