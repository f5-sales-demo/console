schema: urn:f5xc:console:workflow:v1
id: sensitive-data-policy-read
label: Read Sensitive Data Policies
resource: sensitive-data-policy
operation: read
params:
  namespace:
    required: true
    example: demo
  name:
    required: true
    example: example-sensitive-data-policy
steps:
  - id: navigate-to-list
    action: navigate
    url: /web/workspaces/web-app-and-api-protection/namespaces/{namespace}/manage/api_security/sensitive_data_policies
    wait_for: text('Sensitive Data Policies')
    description: Navigate to Sensitive Data Policies list page
  - id: click-resource
    action: click
    selector: row:has-text('{name}') >> link
    description: Click the resource name to open the detail/edit view
    note: The agent reads the page content via get_page_text/javascript_tool after navigation
metadata:
  confidence: generated
  console_version: "2025.06"
  generated_by: scripts/generate-workflows.ts
