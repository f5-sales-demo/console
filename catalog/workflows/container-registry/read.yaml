schema: urn:f5xc:console:workflow:v1
id: container-registry-read
label: Read Container Registries
resource: container-registry
operation: read
params:
  namespace:
    required: true
    example: demo
  name:
    required: true
    example: example-container-registry
steps:
  - id: navigate-to-list
    action: navigate
    url: /web/workspaces/distributed-apps/namespaces/{namespace}/manage/container_registries
    wait_for: text('Container Registries')
    description: Navigate to Container Registries list page
  - id: click-resource
    action: click
    selector: row:has-text('{name}') >> link
    description: Click the resource name to open the detail/edit view
    note: The agent reads the page content via get_page_text/javascript_tool after navigation
metadata:
  confidence: generated
  console_version: "2025.06"
  generated_by: scripts/generate-workflows.ts
